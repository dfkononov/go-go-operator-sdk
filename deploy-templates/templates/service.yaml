apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-operator-sdk-deploy-github.fullname" . }}
  labels:
    {{- include "go-operator-sdk-deploy-github.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-operator-sdk-deploy-github.selectorLabels" . | nindent 4 }}
