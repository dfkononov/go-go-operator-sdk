apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-operator-sdk-deploy-github.fullname" . }}-test-connection"
  labels:
    {{- include "go-operator-sdk-deploy-github.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-operator-sdk-deploy-github.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
